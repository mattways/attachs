class CreateAudios < ActiveRecord::Migration
  def change
    create_table :audios do |t|
      t.string :name, :null => false
      t.string :filename, :null => false
      t.string :mime, :null => false
      t.integer :size, :null => false
      t.integer :audible_id, :null => false
      t.string :audible_type, :null => false
      t.timestamps          
    end    
    add_index :audios, :name
    add_index :audios, :filename
    add_index :audios, :mime
    add_index :audios, :size    
    add_index :audios, :audible_id
    add_index :audios, :audible_type  
    add_index :audios, :created_at
    add_index :audios, :updated_at   
  end
end